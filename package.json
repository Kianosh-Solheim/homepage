{
  "name": "homepage",
  "version": "1.0.0",
  "description": "",
  "main": "App.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild App.js --bundle --outfile=bundle.js --format=esm --loader:.js=jsx --external:react --external:react-dom"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
